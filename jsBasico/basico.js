//Exibibindo uma mensagem no console:

console.log("Isso é uma mensagem");

//Declarando variáveis 

let variavel1 = 'Essa é a variável 1. let, é usado para variáveis que podem ser reatribuaidas mesmo depois de terem sido declaradas';

const variavel2 = 'Essa é a variável 2. const, é usado para variáveis que não podem ser reatribuidas';

var variavel3 = 'Essa é a variável 3. var pode ser reatribuida, porém é descontinuada e não é uma boa prática utiliza-lá';


//operações:
/*
+ adição
- subtração
* multplicação
/ divisão
++ icrement
-- decrement
*/

// comparações:
/*
== igual a 
=== igual a 
=> igual ou maior a 
<= igual ou menor a 
!= diferente de 
> maior que
< menor que 
&& e(and)
|| ou(or)
! inverte a condição
*/

//tipos de dados:
/*
string = texto 
number = número 
int = numero inteiro
float = numero flutuante(decimal)
boolean = true, false(verdadeiro e falso)
null = nulo
underfined = indefinido
object = objeto
array = lista

*/
